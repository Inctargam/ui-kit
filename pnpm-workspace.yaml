# pnpm 11 не запускает postinstall-скрипты зависимостей, пока их явно не разрешишь.
# esbuild приезжает транзитивно с Vite и в своём postinstall подкладывает нативный
# бинарник под платформу — без него сборка падает на старте.
allowBuilds:
  esbuild: true
