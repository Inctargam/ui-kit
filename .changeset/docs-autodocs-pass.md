---
'@remark-gram/ui-kit': patch
---

`ComboboxProps` и `ComboboxOption` получили JSDoc на каждое поле — описания теперь
видны в подсказках редактора и в таблице пропсов витрины. Поведение не изменилось.
