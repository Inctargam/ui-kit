/**
 * Публичный API библиотеки. Всё, что реэкспортировано отсюда, — обещание
 * поддерживать: переименование или удаление это breaking change.
 *
 * Компоненты и их типы пропсов:
 * `Alert`, `Button`, `Card`, `Checkbox`, `DatePicker`, `Input`, `Modal`,
 * `Pagination`, `RadioGroup`, `Recaptcha`, `Scroll`, `Select`, `Tabs`, `TextArea`.
 *
 * Наружу намеренно не идёт:
 * — `Calendar` (`components/date-picker/Calendar.tsx`) — внутренность `DatePicker`,
 *   а не второй компонент кита. Своего продуманного API у него пока нет:
 *   он не управляемый снаружи по месяцу, не умеет min/max и локали. Экспорт
 *   заморозил бы всё это в текущем виде;
 * — `components/shared/selectionControl.module.css` — общие классы `Checkbox`
 *   и `RadioGroup`. CSS Modules подключаются через `composes`, импортировать
 *   их потребителю незачем;
 * — внутренние хелперы компонентов (построение сетки дней, расчёт диапазона
 *   номеров страниц) — они лежат в файлах компонентов и не экспортируются;
 * — сами CSS-модули компонентов: имена классов хешируются при сборке,
 *   опираться на них снаружи нельзя. Стили приезжают одним `styles.css`;
 * — стори (`*.stories.tsx`): `tsconfig.build.json` их не собирает, в `dist/`
 *   они не попадают.
 */

export * from './components'
// Набор иконок: 90 компонентов, `IconProps` и union `IconName`.
// Реэкспорт пофайловый, поэтому tree-shaking не ломается — потребитель
// платит только за те иконки, которые импортировал.
export * from './icons'
